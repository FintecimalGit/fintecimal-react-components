import React from 'react';
import PropTypes from 'prop-types';

const RFCInput = props => {
    return (
        <div>
            
        </div>
    );
};

RFCInput.propTypes = {
    
};

export default RFCInput;