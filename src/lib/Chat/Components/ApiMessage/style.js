import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  container: {
    width: '100%',
    color: '#4F41F2',
    fontSize: '1rem',
    fontWeight: 600,
    lineHeight: '22px',
    textAlign: 'right',
    fontFamily: 'Open Sans',
  },
}));
