import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  container: {
    width: '100%',
    color: '#7D7D7D',
    fontSize: '1rem',
    fontWeight: 600,
    lineHeight: '22px',
    textAlign: 'left',
    fontFamily: 'Open Sans',
  },
}));
