import React from './node_modules/react';
import PropTypes from './node_modules/prop-types';

const CURPInput = props => {
    return (
        <div>
            
        </div>
    );
};

CURPInput.propTypes = {
    
};

export default CURPInput;